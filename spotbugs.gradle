apply plugin: "com.github.spotbugs"

spotbugs {
    toolVersion = '4.0.1'
    sourceSets = [sourceSets.main]
    effort = 'max'
    reportLevel = 'high'
}

spotbugsMain {
    reports {
        html.enabled(true)
        xml.enabled(false)
    }
}
