plugins {
  id "com.github.spotbugs" version "5.0.14"
}

spotbugs {
    toolVersion = '3.1.12'
    sourceSets = [sourceSets.main]
    effort = 'max'
    reportLevel = 'high'
}

spotbugsMain {
    reports {
        html.enabled(true)
        xml.enabled(false)
    }
}
